# Phase 02.2: CI and build-smoke matrix - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-14
**Phase:** 02.2-ci-and-build-smoke-matrix
**Areas discussed:** Render smoke check, Windows CI job, GL-capability diagnostic, CI dependency provisioning

---

## Render smoke check

| Option | Description | Selected |
|--------|-------------|----------|
| Reuse exportPNGAfterTTL | Launch BALLView with the startup demo + the existing TTL PNG-export path, assert the PNG is non-blank. Reuses existing code. | ✓ |
| New tiny GL test harness | Standalone test creating a QOpenGLWidget/offscreen surface, render one frame, grab framebuffer. More isolated, new code. | |
| Just build, no render assert | CI only proves compile+link on 3 OSes; defer the pixel check. | |

**User's choice:** Reuse exportPNGAfterTTL (recommended)
**Notes:** Reuses code BALL already has; least new surface to maintain.

---

## Windows CI job

| Option | Description | Selected |
|--------|-------------|----------|
| Present but non-blocking | Windows job runs from day one as allow-failure / continue-on-error; flip to required once Phase 4's vcpkg manifest lands. | ✓ |
| Omit until Phase 4 | macOS + Linux only for now; add Windows in Phase 4. | |
| Blocking now | Required Windows build immediately, needs ad-hoc Windows dep setup before Phase 4. | |

**User's choice:** Present but non-blocking (recommended)
**Notes:** Keeps Windows visible without blocking the milestone before the vcpkg manifest exists.

---

## GL-capability diagnostic (DIAG-01)

| Option | Description | Selected |
|--------|-------------|----------|
| stdout, one machine-parseable line | Single structured line at startup (GL vendor/version/profile, QSurfaceFormat, DPR, default FBO size, backend). Easy for the smoke check to grep. | ✓ |
| Both stdout + BALLView log panel | Same data, also shown in the in-app Logs panel. More user-visible, more wiring. | |
| Dedicated diagnostics file | Write to a `BALLView-gldiag.txt`. Cleaner separation, smoke check must locate the file. | |

**User's choice:** stdout, one machine-parseable line (recommended)
**Notes:** Doubles as the smoke check's oracle; targets the exact fields that would have caught the Phase 2 GL-context regressions.

---

## CI dependency provisioning

| Option | Description | Selected |
|--------|-------------|----------|
| Native pkg mgrs + caching | macOS: Homebrew with caching; Linux: apt. Matches BUILD-macos.md exactly — CI tests the same path contributors use. | ✓ |
| install-qt-action for Qt | Prebuilt Qt via jurplel/install-qt-action, system pkgs for the rest. Faster Qt, diverges from documented flow. | |
| You decide | Defer to researcher/planner. | |

**User's choice:** Native pkg mgrs + caching (recommended)
**Notes:** CI exercises the documented Homebrew flow rather than a divergent path.

---

## Claude's Discretion

- Linux headless GL approach (xvfb + Mesa llvmpipe / `LIBGL_ALWAYS_SOFTWARE`) — implementation detail for the planner/researcher.
- CI cache keys, job/matrix YAML structure, configure/build/smoke step split.
- Whether the grep-gate lint is its own job or a step per build job.
- Build scope anchored (BALL+VIEW+BALLView, Release, Python/RTfact OFF, on push+PR) but exact runner labels confirmed at planning.

## Deferred Ideas

- `test/` tree → `ctest` wiring (Phase 9).
- vcpkg manifest (Phase 4).
- `CMakePresets.json` (Phase 4 / DEPS-05).
- Golden-image pixel-diff regression (later enhancement).
- Ninja generator for CI (backlog 999.2).
